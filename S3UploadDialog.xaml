<Window x:Class="AwsSecurityAssessment.S3UploadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload to S3" Height="200" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="S3 Bucket Name:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="BucketNameTextBox" Margin="0,0,0,15"/>

        <Label Grid.Row="2" Content="Key Prefix (optional):" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="KeyPrefixTextBox" Text="security-assessments/" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="CancelButton_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Upload" Click="UploadButton_Click" Padding="15,8" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

